name: Generate Headlines

on:
  schedule:
    # Run daily at 5:00 AM UTC
    - cron: '0 5 * * *'
  # Allow manual trigger
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Generate headlines (All ranges)
        run: |
          mkdir -p docs
          node src/cli.js --range today --outdir docs
          node src/cli.js --range week --outdir docs
          node src/cli.js --range month --outdir docs
          node src/cli.js --range all --outdir docs
          cp docs/week.html docs/index.html
          # Also copy to repo root (GitHub Pages serves from root)
          cp docs/today.html today.html
          cp docs/week.html week.html
          cp docs/month.html month.html
          cp docs/quarter.html quarter.html
          cp docs/index.html index.html

      - name: Commit & push changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add docs/ today.html week.html month.html quarter.html index.html
          git diff --staged --quiet || (git commit -m "Auto-generate headlines $(date -u '+%Y-%m-%d %H:%M UTC')" && git push)

      - name: Done!
        run: echo "Headlines updated successfully!"
